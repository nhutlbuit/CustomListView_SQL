<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nhutl.sqlexample">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ListProduct"></activity>
        <activity android:name=".activity.FlashLight"></activity>
        <activity android:name=".activity.Screen_colors"></activity>
        <activity android:name=".activity.Part"></activity>
        <activity android:name=".activity.LockScreen"></activity>
        <activity android:name=".activity.XuXuDungKhoc"></activity>
        <activity android:name=".activity.Web_viewXuxuDungKhoc"></activity>
    </application>

</manifest>